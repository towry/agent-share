Add an unread-notifications bell to src/components/Header.tsx — the same unread list also feeds SettingsInbox.tsx and the nightly email digest job, and right now each of those would end up calling GET /api/notifications on its own.
Add a --format=json flag to the deploy command in src/cli/commands/deploy.ts, where the output is printed by three subcommands that each parse process.argv separately and each call process.exit with their own code.
Write the nightly job that merges orders.parquet into the customer_dim table in BigQuery — orders.customer_id is null on about 4% of rows, and the job gets re-run for the last three days whenever an upstream load fails.
Put a Redis cache in front of GET /pricing in services/pricing/main.go — we run three replicas behind the ALB and the promo_rate flag gets flipped from the admin console at unpredictable times during the day.
Add offline caching to the trip list behind TripListViewModel.kt — those trips are also drawn by the home widget and the share sheet, an empty list has to render the 'no trips yet' card, and a WorkManager sync can land while the screen is open.
Add a retry option to fetchJson in packages/http/src/index.ts — it's only called from our own auth client today, callers already wrap it in try/catch, and the errors it throws are plain Error objects with no status code attached.
Add a --shard=1/6 flag to tools/e2e/runner.ts so CI can split the 400 specs across six machines — every spec file logs in through the same session fixture and seeds the same Postgres database in beforeAll.
Set up the docs site for the SDK under docs/ — there are 30 markdown pages, four autogenerated API references, and the same component prop table copy-pasted into 12 of the pages.
