I need to migrate every axios call in src/api/ (about 40 sites across auth.ts, billing.ts, and the webhook handlers) to the new fetchJson wrapper in src/lib/http.ts, and I was going to just sweep them all and run the suite at the end, except the retry-on-401 path in auth.ts is the one place where a silent break would only show up in production.
Rename the --dry-run flag to --plan across all twelve commander subcommands in src/commands/ and update docs/cli.md, but migrate.ts and restore.ts both build their options through the shared builder in src/commands/options.ts, so I don't want to find out at the end which subcommands still silently accept --dry-run.
Backfill orders.customer_id across 90M rows in the Postgres 15 cluster with a chunked UPDATE loop, and I'd rather not discover three hours in that the join against legacy_accounts drops the ~4k orphaned accounts that have a null in customer_ref.
Roll the new terraform/modules/rds module out to all fifteen service roots in infra/envs/prod/, but each root has hand-edited instance_class and two still point at the decommed subnet group, so I can't tell which ones will try to recreate the database until I read a plan.
Switch the twenty ViewModels under app/src/feature/*/ from RxJava to Kotlin Flow without changing behaviour, and CheckoutViewModel.kt also backs the silent token-refresh path that the homescreen widget calls, so I need to know that one still works before I touch the other nineteen.
Deprecate parseDuration(String) in favour of Duration.parse(...) across the SDK's six public entry points and update the generated .d.ts plus the migration guide, and since it's published with 3k dependents I want the type errors and the runtime deprecation warning to surface as I go rather than after the whole refactor lands.
Replace the jest.mock('../api/client') calls in about 60 spec files under test/unit/ with MSW handlers in test/msw/handlers.ts, though a handful of them use mockRejectedValueOnce to simulate a 500 and assert the retry in src/api/retry.ts, which is exactly the behaviour that disappears without anyone noticing.
Rewrite the 30 v1 endpoint pages under docs/api/ against the new OpenAPI spec at openapi/v2.yaml, and I want each page's generated snippet diffed against the real server response because the pagination shape changed on three endpoints and a stale example is worse than no page.
