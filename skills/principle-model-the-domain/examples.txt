I'm adding a 'partial refund' action to the checkout page in `CheckoutStatus.tsx`, but `isPaid`, `isShipped`, and `canRefund` are toggled in three separate handlers across `OrderSummary`, `RefundButton`, and `api/orders.ts`, and now a cancelled order can still show the refund button, so I need to rework how order progress is tracked.
I need to add a `--resume` flag to our `deploy` CLI in `src/cli/deploy.ts`, but `--dry-run`, `--force`, `--skip-tests`, and `--rollback` are checked in four different places and `--resume` conflicts with `--rollback` only when `--force` is false, so please clean up how the command decides what to run.
I'm adding a new `source='adyen'` to our payments ETL, but `validate_row.py`, `transform.py`, and `load.py` each have their own `if source == 'stripe'` or `'paypal'` checks and I keep forgetting to update one, so I need the source rules to live in one place.
I need to add a `staging-eu` environment to our Terraform module, but `main.tf` already has nested `count` conditions for `prod`, `staging`, and `dev`, and the S3 bucket naming logic is duplicated in `outputs.tf`, so I want to stop copying those checks each time.
I'm wiring up a new 'order delayed' push notification in `OrderStatusViewController.swift`, and the screen currently toggles `isPreparing`, `isReady`, and `isDelivered` in four places while the retry path in `NotificationService.swift` assumes only one of them is true, so I need to make the order progress consistent.
I'm adding Japanese era support to our `dateparser` library, but `parse_date` already branches on `tz`, `locale`, and `strict` across `formats.py` and `normalize.py`, and the new era names overlap with the existing `reiwa` handling, so please reorganize how parsing decisions are made.
I'm adding a `slow_mo` option to our `browser_session` pytest fixture in `conftest.py`, but the fixture already uses `headless`, `mobile`, and `record_video` booleans to decide setup and teardown in `browser_fixture.py`, and the `--headed` CLI flag now breaks when `mobile` is true, so I need to untangle how browser options are combined.
I'm updating our docs generator to emit a 'migration' section, but `generate_docs.py` has separate `if doc_type == 'api'`, `'guide'`, and `'changelog'` blocks in `render_page`, `build_nav`, and `validate_frontmatter`, and the new section needs the same frontmatter rules, so I want to stop repeating those checks.
