In apps/web/src/hooks/useCheckout.ts the state is `{ loading: boolean; order?: Order; error?: string }` and QA just filed a bug where the spinner and the error banner render at the same time on a failed retry — can you rework that hook and <CheckoutStatus /> so that combination can't be produced?
Our `acme deploy` command takes `--user-id` and `--order-id` as plain strings that both flow into `resolveTarget()` in packages/cli/src/args.ts, and last week someone shipped `deploy --user-id o_123` and quietly deployed into the wrong tenant — can you make the CLI catch that swap before it ever calls the API?
The nightly job jobs/ingest_orders.py does `json.loads(raw)["items"]` straight off the Kafka topic, and Tuesday it died with a KeyError on 400k rows because the upstream producer renamed the field to `line_items` with no notice — can you add a decode step in transform.py so a malformed or renamed payload surfaces as a clear error at the edge instead of three functions deep?
In infra/modules/service/main.tf we expose `var.scaling` with `enabled`, `min`, and `max`, and a teammate set `enabled = false` with `min = 3`; Terraform planned it happily and we only noticed in the apply output — can you restructure that module's inputs so `terraform validate` refuses the combination?
android/app/src/main/java/com/acme/pay/PaymentScreen.kt holds `PaymentUiState(method: Method, cardToken: String?, bankAccount: String?)` and we get a crash when `method == CARD` and `cardToken` is null after the user backs out of the form — can you rework PaymentUiState and the ViewModel so the screen never has to null-check the field for the selected method?
The public API of @acme/orders-client is `createOrder(orderId: string, customerId: string)` and we've swapped the two arguments at three call sites already, but the JSON body we POST to the gateway is frozen by the vendor contract — can you change the client's signatures so the swap can't be written, without touching the wire format?
packages/test-utils/src/assert-event.ts switches on `event.type` with a default that just logs, so when the team added the `payment.refunded` event last sprint thirty integration tests passed while asserting nothing about it — can you change that helper so the next new event type breaks the build instead of passing quietly?
docs/reference/cli.md has a hand-maintained flags table that's drifted from packages/cli/src/flags.ts again — `--dry-run` shipped two releases ago and still isn't documented — can you generate that table from the flag definitions during the docs build so it can't drift?
