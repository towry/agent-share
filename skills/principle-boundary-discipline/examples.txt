In `app/api/webhooks/stripe/route.ts`, I need to parse the incoming Stripe event into a `PaymentEvent` and hand it to `applyPaymentEvent`, because Stripe retries can send the same event twice and `applyPaymentEvent` currently calls `stripe.webhooks.constructEvent` again for every line item, which blows up on the replay.
In `cmd/deploy/main.go`, I want to read and parse the `--config` flag into a `DeployConfig` once in `main()` and pass it to `runDeploy`, since `runDeploy` is called by both the `deploy` and `rollback` subcommands and each currently checks if `cfg.Cluster` is empty before every kubectl call.
In `pipelines/orders/etl.py`, please parse the raw CSV rows into `Order` dataclasses in `load_orders` and let `compute_totals(orders)` use `order.amount` directly, because `compute_totals` is also imported by a Jupyter notebook that passes strings and the current `float(order['amount'])` inside the loop fails on the empty `amount` column for refunds.
In `infra/modules/rds/main.tf`, I need `local.db_identifier` to assume `var.instance_class` is already a valid string from the root module and drop the regex `can()` check inside it, because that local is reused by the read replica resource and the root module already rejects bad values in `variables.tf`.
In `ios/Checkout/CheckoutViewModel.swift`, I want `CheckoutAPI.fetchCart()` to decode the `/cart` JSON into a `Cart` struct and have `applyPromoCode(promo:)` use `cart.items` directly, because the text field calls `applyPromoCode` on every keystroke and the current code re-decodes `cartJSON` each time, causing a visible lag on the iPhone SE.
In `pkg/geohash/encode.go`, I need to keep `Encode(lat, lng float64)` returning a `string` instead of exposing the internal `cell` struct in the public API, since three downstream services import `Encode` and changing the signature to return `cell` would force them to import `pkg/geohash/internal` and break their builds.
In `test-utils/fixtures.ts`, I want the Playwright fixture to parse `process.env.TEST_USER` into a `User` object once before `test.beforeEach` runs, because 40 spec files import `makeUser()` and each call currently does `JSON.parse(process.env.TEST_USER!)` inside the test body, making the suite re-parse the same JSON 40 times.
In `docs/plugins/generate-api.mdx`, I need `renderEndpoint(endpoint: OpenAPIObject)` to receive the OpenAPI spec as a typed argument instead of reading `process.env.OPENAPI_URL` inside it, since the MDX component renders for every one of the 200 endpoint pages and currently fetches the spec from the network each time, hitting the rate limit.
