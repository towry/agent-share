I want to update `src/components/CheckoutButton.tsx` so the button disables when `cart.items.length === 0`, add a Vitest case for that empty-cart state in `CheckoutButton.test.tsx`, and adjust the single caller in `CheckoutPage.tsx` because no other component imports it.
I want to add a `--json` flag to the `backup restore` command in `cmd/backup/restore.go` and update the golden files in `testdata/restore/` so scripts can parse the restored snapshot ID, while leaving the existing human-readable output unchanged because the flag is opt-in.
I want to write `models/marts/dim_customer.sql` to coalesce null `region_code` values from the Salesforce export to `'UNKNOWN'` and add a `not_null` test in `models/marts/schema.yml`, since the downstream BI dashboard already handles unknown regions and the nulls are only about 2% of accounts.
I want to bump the `engine_version` in `terraform/modules/rds/main.tf` from 14.7 to 14.10 and add `prevent_destroy` on the cluster, because staging already runs 14.10 and the only consumer is the internal reporting service.
I want to change `LoginScreen.kt` so a `NetworkException` from `authViewModel.error` shows a snackbar instead of a full-screen error, and add a Compose UI test in `LoginScreenTest.kt`; this only touches the error branch for one screen and no API contract.
I want to add an optional `retry` parameter to `fetchWithAuth` in `src/http/client.ts`, export it from `src/index.ts` with a default of zero, and update the only internal caller `SyncService.ts` to pass `retry: 2` in the same commit so existing consumers stay unchanged.
I want to set `retries: 2` for the `chromium` project in `playwright.config.ts` and add a `test.extend` fixture in `tests/fixtures.ts` that logs console errors on failure, because `checkout.spec.ts` is flaky only on CI and we need the trace before the next release.
I want to rewrite `docs/api/authentication.md` to replace deprecated `X-API-Key` examples with `Authorization: Bearer <token>` and update `examples/curl/auth.sh`, since the auth service already accepts bearer tokens and the old examples are generating support tickets.
