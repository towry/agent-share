I need to harden the Stripe webhook handler in apps/web/src/pages/api/stripe-webhook.ts so that when checkout.session.completed is re-delivered from the Stripe dashboard retry button, it doesn't insert a second row into the shipments table for the same checkout session id.
I want the `fleetctl deploy` command in cmd/deploy.go to clean up before starting when I Ctrl-C it midway through writing ~/.kube/fleet-context and then immediately run the same command again, because right now it leaves a port-forward process and a duplicate context entry.
Please fix the Airflow task `load_events_to_s3` in dags/events_dag.py so rerunning the 2024-05-01 backfill after a worker OOM doesn't leave duplicate parquet files under s3://warehouse/events/dt=2024-05-01/.
I need the Terraform module in infra/modules/github_oidc to recover when `terraform apply` dies after creating the aws_iam_role but before attaching the managed policy, so rerunning doesn't fail with EntityAlreadyExists.
I need the Android `PhotoUploadWorker` in app/src/main/java/com/example/sync/PhotoUploadWorker.kt to avoid uploading the same local photo twice when WorkManager reschedules it after the phone reboots mid-upload.
In the `@acme/logger` package, I want `configureLogger()` in src/index.ts to handle a consumer calling it once in `main.ts` and again in a lazy-loaded module, so we don't register the same Sentry transport twice.
I need the Playwright `globalSetup` in e2e/global-setup.ts to stop failing on duplicate key value violates unique constraint 'users_email_key' when CI retries the suite after a flaky login test.
I want the `npm run docs:api` script in scripts/generate-api-docs.ts to replace the generated section in docs/api.md instead of appending another copy when I run it twice while editing the OpenAPI spec.
