I'm swapping the Redux session store in src/store/session.ts for React Query across the checkout flow, and since the old path only runs for the 3% of users behind the `checkout_v2` flag that we're turning off next sprint, I want to rip out the selectors and the dual-write in useSession.ts rather than keep it working for both flag states.
I'm rewriting argument parsing in cli/src/commands/deploy.ts from commander to yargs, and since the only callers are scripts/deploy-staging.sh and scripts/deploy-prod.sh which I'm editing in the same PR, I don't want to keep a flag-translation layer mapping `--env` onto `--environment` for anyone still on the old binary.
We're porting the nightly ETL from jobs/nightly_rollup.py to dbt models under models/marts/, and because the only downstream reader is the Metabase 'Revenue Ops' dashboard that I can repoint in an afternoon, I'd rather delete the pandas job than keep its output tables in sync with the dbt ones for a month.
I'm splitting the payments VPC out of terraform/monolith into terraform/stacks/payments/ using `terraform state mv`, and since that VPC only holds the batch-worker service that already has a Sunday maintenance window, I want one weekend cutover instead of standing up two VPCs joined by a Transit Gateway for a quarter.
I'm replacing the deprecated wrapper in Auth/KeychainStore.swift with SecureEnclaveAuth, and because the app is still pre-1.0 and we own the only backend at auth.internal/v2, I want to drop the code that migrates old keychain items on first launch and require a fresh sign-in.
I'm changing @acme/date-utils from moment-style mutable Dates to Temporal-based immutable values, and since we're on 0.x with the scheduling service as the single consumer, I want to publish v0.9 with only the new API instead of shipping both overloads behind a deprecation flag.
I'm moving our 400 spec files from the hand-rolled Jest setup in test/setup/polyfills.ts to Vitest's built-in jsdom environment, and since the only genuinely fiddly polyfill is crypto.randomUUID which Vitest provides natively, I want to delete the file and fix whatever specs break instead of leaving a conditional fallback wired into setupFiles.
I'm restructuring docs/ from the flat MkDocs layout into tutorials/, how-to/, reference/, explanation/ with a new nav in mkdocs.yml, and because the old URLs only live in a few Slack pins and one README link, I plan to delete the old pages and ship a single redirects map instead of keeping each one as a stub pointing at its replacement.
