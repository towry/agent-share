I want to add multi-tenant org scoping to our Express API — right now `req.user` is global, the single Redis session namespace is hardcoded in `config/session.js`, and every query in `routes/` filters only by `userId`.
I need to add a `--json` flag to the `status` subcommand in `cli/main.py`; all twelve commands currently build and print a human table inline through the shared `render_table()` helper, so there's no structured result to serialize.
I want to switch our Airflow DAG `dags/load_events.py` from full nightly truncate-reload to incremental upserts, but the original schema has no `updated_at`, the dbt models downstream all select `*` from `events`, and only the `orders` source exposes a reliable cursor.
I need to add a staging environment to our Terraform in `infra/`, but every resource name, the S3 bucket, the IAM role, and the single `terraform.tfvars` are hardcoded with `prod`, and the backend key is literally `prod/terraform.tfstate`.
I want to add offline support to `SyncRepository.kt` in our Android app; every screen calls `api.fetch()` and renders a spinner, the only cache is an in-memory `Map` cleared on process death, and the login flow depends on a token-refresh call that assumes connectivity.
I need to make `parsecsv.parse()` accept a file-like stream, but it currently loads the entire file into one `Document` object and every public function (`filter_rows`, `write_csv`, `describe`) takes that `Document` as its first argument.
I want to add per-test timeouts to our pytest plugin `pytest_timeout_plugin.py`, but fixture setup and teardown are wrapped in a single `yield` in `conftest.py`, and the reporting hook assumes each test item has exactly one duration, which breaks when a flaky test is retried with the same nodeid.
I need to add versioned docs to our MkDocs site, but the left nav is a single hand-maintained `nav:` list in `mkdocs.yml`, every page hardcodes `latest` in its canonical URL and cross-links, and there's only one version folder today.
