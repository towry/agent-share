I need to fix the `useUserProfile` hook so the avatar falls back to a default when `avatar_url` is null, but the API response for `/api/users/me` includes a 2 MB `activity_feed` array I don't need for this empty-state fix.
I need to add a `--dry-run` flag to the `deploy` command in `cli/src/commands/deploy.ts`, but the existing command prints the entire 50,000-line Kubernetes manifest diff on every retry and I only need the first line that says whether the deployment changed.
I need to write a SQL query to find duplicate `email` values in the `users` table, but the table has 80 million rows and the first `SELECT *` returned a 10 GB CSV; I only need the `email` and `id` columns for rows where `email` appears more than once.
I need to update the `aws_iam_policy` for the `lambda_worker` in `infra/modules/worker/main.tf`, but `terraform plan` shows 12,000 lines of unrelated drift and the only changed block is the `s3:GetObject` statement.
I need to fix the `UITableView` crash in `FeedViewController.swift` when `posts` is empty and the app retries the `/feed` request, but the Xcode console logs every cell's `NSIndexPath` for 10,000 rows and I only need the stack trace at the `reloadData()` call.
I need to add a `TimeoutError` class to `src/errors.ts` in our `http-client` package, but the package has 80 modules and `TimeoutError` is only referenced by `request.ts` and its own test file.
I need to update the Playwright test `checkout.spec.ts` to use the new `data-testid` for the submit button, but running `npx playwright test --ui` shows a trace with 5,000 network requests and I only need the DOM snapshot at the click event.
I need to rewrite the `POST /v2/orders` section of `docs/openapi.yaml`, but the generated spec is 1.5 MB and the only endpoint that changed is that one, with a new `idempotency_key` header.
