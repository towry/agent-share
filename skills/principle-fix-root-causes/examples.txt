Our POST /api/orders handler 500s whenever the request body has no `items` array, the stack trace lands in `orderTotal()` in `src/cart.js`, and that same helper is called by the Stripe webhook and the CSV importer — I want the endpoint to stop throwing so we can ship checkout this sprint.
`mytool deploy` exits 1 with `ENOENT: .mytoolrc` on GitHub Actions but works on my laptop, and since the config file is optional anyway I'd like `loadConfig()` in `packages/cli/src/config.ts` to fall back to defaults when it's missing — note that `mytool status` and `mytool logs` call the same loader.
The nightly `load_events` Airflow DAG has emitted duplicate rows ever since we started rerunning failed tasks, and a `SELECT DISTINCT` in `dbt/models/fct_events.sql` would close the 4% delta on the revenue dashboard before Monday's review.
Pods in the `api` deployment get OOMKilled roughly every six hours since the 3.4 release, so I'd like to raise `resources.limits.memory` in `k8s/api-deployment.yaml` from 512Mi to 2Gi in time for the launch window.
On the 3.2 release branch, tapping Save in `ProfileEditView.swift` with an email that's already registered does nothing at all — no alert, no spinner, no error — and the team wants to hide the Save button whenever the field is unchanged so QA can sign off.
`parseDuration('1h30')` in `src/duration.ts` returns NaN and issue #212 is asking for a fix, so I'm inclined to make it return 0 for any string it can't parse and cut 2.8.1 today.
`checkout.spec.ts` fails about 1 in 5 CI runs but never on my machine, and adding `jest.retryTimes(3)` to `jest.setup.ts` would let the release branch merge tonight.
`docs/getting-started.md` step 2 runs `make setup`, which fails on M-series Macs because the pinned `postgres:11` image has no arm64 build, and I want to add a line under that step telling readers to ignore the error — half the team is on Apple Silicon now.
