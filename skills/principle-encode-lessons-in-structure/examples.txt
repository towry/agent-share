I've now told you in three consecutive PRs that any new field on the `Payment` type in `packages/api-types/src/payment.ts` also needs an entry in `packages/api-types/src/schema.ts` and in `packages/web/src/fixtures/payments.json`, and this morning's `refundReason` addition shipped without either so every row on the staging refunds table renders `undefined` — can we make this stop happening?
Every subcommand flag I add to `cmd/deploy.go` has to be mirrored into the `--help` golden file at `testdata/help/deploy.txt` and the table in `docs/cli-reference.md`, and I've reminded you of that twice already this week; last release went out with `--canary-weight` completely undocumented and support fielded four tickets about it — please take care of this for me.
I keep having to remind you that adding a column to `models/staging/stg_orders.sql` means updating `models/staging/schema.yml` and the downstream `fct_orders` model; the `discount_cents` column we merged on Tuesday has no not-null assertion, and nulls from a partial backfill quietly under-reported revenue for two days — handle the next one end to end.
I've said it three times now: any new bucket in `infra/modules/storage/main.tf` needs the access-logging block and the `deny-insecure-transport` policy attached; the analytics bucket added on Tuesday has neither and the security review marked it as a release blocker — can you sort the whole thing out?
Every screen I add under `app/src/screens/` has to be registered in `navigation/RootNavigator.tsx` and listed in the `deepLinks` array in `app.config.ts`; I corrected you on this for `CheckoutScreen` and again for `RefundScreen`, and now the refund push notification opens the app to a blank screen — please deal with it.
In `packages/sdk` any new public function needs re-exporting from `src/index.ts` and adding to the `exports` map in `package.json`; we've shipped two minor versions where `createWebhookSignature` worked in our own tests but consumers couldn't import it, and I've asked you about this in every session since — can we close this off?
Every time we drop new fixtures under `tests/fixtures/invoices/` the `fixtureVersion` constant in `tests/setup.ts` needs bumping, and I've had to say that in four separate sessions; last night's fixtures are being parsed with the old schema in CI and seven specs are red — please handle it.
Whenever a plugin gets added to the `plugins` array in `gatsby-config.ts` we need a matching row in the `docs/configuration.md` table; I've left that same review comment on three consecutive PRs and `indexName` for the search plugin is still undocumented while support keeps getting tickets — can you take it from here?
