I'm adding a 'Save draft' button to CheckoutForm.tsx in our React storefront, and I need to decide whether to keep the current optimistic UI that instantly clears the textarea or instead show a brief inline 'Saved' state and disable the button while the POST /api/drafts request is in flight, because on slow mobile connections users double-submit and lose their typed address.
I'm implementing the `deploy` subcommand in cmd/deploy.go for our Go CLI, and I need to decide whether to print a single terse 'ok' line or a step-by-step progress view with the current Kubernetes resource and elapsed time, since CI captures stdout but a human also runs it locally during incidents.
I'm writing the Airflow DAG daily_customer_rollup that populates analytics.customer_daily, and I need to decide whether to add a `--dry-run` task that prints row counts and a schema diff before the real load, even though it duplicates the SELECT and the only downstream consumer is the BI dashboard.
I'm updating infra/prod/eks.tf and the accompanying Makefile, and I need to decide whether to add a `make doctor` target that validates AWS creds, kubectl context, and Helm chart versions before `terraform apply`, because the current one-command apply saves setup time but caused three failed rollouts this sprint.
I'm building the offline retry path in OrderSyncService.swift for our iOS app, and I need to decide whether to surface a non-blocking banner with a 'Retry now' button when sync fails or silently wait for the next scheduled attempt, since the API client already has exponential backoff but users may think their order vanished.
I'm designing the public signature for parseConfig() in our TypeScript config library, and I need to decide whether to accept a plain options object with defaults or require a ConfigBuilder class, because only our three internal services call it today but it will be published to npm next month.
I'm adding snapshot tests for Button.test.tsx in our Vitest setup, and I need to decide whether to assert the full rendered DOM including aria-* attributes and focus-visible styles or just the visible label, because the test runner output is already noisy and a failing snapshot is hard for the next maintainer to review.
I'm writing the README quickstart for @acme/sdk, and I need to decide whether to keep the single 80-line code block that copies every step or split it into a three-step tutorial with expected output and a troubleshooting section, since the repo is private and the only reader is our solutions engineer.
