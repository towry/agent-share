I'm replacing the `getUser` controller in `src/api/users.ts` with `fetchUser` and only `ProfilePage.tsx` and `UserMenu.tsx` still call `getUser`; update those two components and remove `getUser` so we don't ship two endpoints that diverge.
I've added `readConfig()` in `cli/config.py` to replace `loadConfig()`, and only `cli/commands/deploy.py` imports `loadConfig`; switch that command over and delete `loadConfig()` plus its unit test so `--dry-run` stops hitting the stale parser.
I'm rolling out `normalize_events_v2` in `etl/transforms.py` and the only remaining caller of `normalize_events_v1` is the backfill DAG in `dags/backfill_events.py`; update that DAG and drop the v1 function and its fixtures before the nightly run.
I've written `modules/secure_bucket` to replace `modules/s3_bucket` and only `stacks/analytics/main.tf` still references `modules/s3_bucket`; update that stack and delete `modules/s3_bucket` so `terraform plan` doesn't keep proposing the prior encryption defaults.
I'm moving `LoginFragment.kt` to the new `AuthViewModel` and only that fragment still references `LoginViewModelV1`; update the fragment, remove `LoginViewModelV1`, and delete its tests so the empty-state error isn't handled twice.
I'm replacing `parse_v1()` with `parse_v2()` in our internal `config-parser` package, and the only consumer left is `services/billing` in this monorepo; update that import and delete `parse_v1()` so we don't publish two parsers with different number handling.
I've added `assert_snapshot_v2()` to `testing/snapshots.py` and only `tests/test_reports.py` still calls `assert_snapshot_v1()`; update that test module and remove `assert_snapshot_v1()` so failing snapshots stop writing both `.snap` and `.snap.new` files.
I'm changing the docs generator in `docs/build.py` from `render_api_v1()` to `render_api_v2()` and only `docs/templates/api_reference.html` still uses the v1 renderer; update that template and delete `render_api_v1()` so the sidebar links stop pointing at `/api/v1/` pages that no longer exist.
