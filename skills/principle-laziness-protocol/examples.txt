In `src/components/UserProfile.tsx`, I want to show a loading skeleton, so can you add a `showSkeleton` prop to `UserProfile`, also add it to `ProfileLayout` and `ProfileHeader`, and update `useProfileData` to return a new `isInitialLoading` flag even though the skeleton only renders in the empty profile state?
In `cmd/deploy.go`, I need to support a `--dry-run` flag, so can you add it to the `deploy` subcommand, the `runDeploy` function, and the `terraformApply` wrapper even though only the top-level command reads it and `terraformApply` is called from one place?
Our Airflow DAG `daily_sales_etl` has `extract_orders`, `transform_orders`, and `load_orders` each applying the same date-partition filter; I need to add a new `region` filter for one client, so can you add it to all three tasks and their shared config?
In `infra/modules/networking/main.tf`, I want to add a new `environment` tag to every resource, so please update all 14 `aws_subnet`, `aws_security_group`, and `aws_route_table` blocks even though the tag is only used by a single monitoring script that reads the VPC tags.
I need the empty cart state in `ios/Checkout/CheckoutViewController.swift` to show an illustration; can you add a `showEmptyIllustration` boolean to `CheckoutViewModel`, update `CheckoutCoordinator` to supply it, and give `CheckoutView` a parameter for it even though the empty state is only reached when the cart has zero items and the view already knows the item count?
I need to expose a new `parse_with_offsets` function from `lib/src/parser.rs`; can you add a `with_offsets` flag to `ParserOptions`, update `parse`, `parse_stream`, and `parse_file` to accept it, and return new `ParseResult` variants even though only the new public function needs offsets and the other callers never set it?
I need the `db_connection` fixture in `tests/conftest.py` to support a `read_only` mode for one new test; can you add a `read_only` parameter to `db_connection`, `db_transaction`, `db_cursor`, and `db_session` even though only `test_read_replica_lag` passes `read_only=True` and the other fixtures never use it?
In `docs/api/authentication.md`, I need to add a note about token expiry for the `POST /auth/refresh` endpoint; can you create a new `_includes/token-expiry.md` partial, update `_config.yml` to register it, and add a custom Jekyll plugin to inject it into every endpoint page even though only the refresh endpoint has this behavior?
