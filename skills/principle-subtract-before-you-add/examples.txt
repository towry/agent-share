I need to add OAuth login to the React app, but AuthContext.tsx still has a half-built SAML flow and a legacyTokenRefresh effect that only one route calls, so I want to wire the new provider without carrying those along.
I want to add a --dry-run flag to our deploy command, but arg_parser.py already defines three hidden flags from an abandoned rollback feature and a --region option that just prints 'not implemented', so I need to implement dry-run on the current argument surface.
I need to add a session_events table to the analytics pipeline, but dags/etl_sessions.py still schedules a disabled backfill task and loader.py has a schema validator for the dropped legacy_session column, so I want to build the table against the actual upstream schema.
I'm adding a Kubernetes HPA for the checkout service, but deployment.yaml still includes an unused sidecar-proxy container and a ConfigMap key for a retired payment gateway, so I need to define the HPA around the live containers.
I want to add dark mode to the iOS settings screen, but ThemeManager.swift still exposes an experimentalHighContrast path with no UI and the storyboard has an empty legacyAppearance view controller, so I need to implement dark mode from the current theme model.
I need to add a ParseDuration function to our Go time utility package, but legacy_parse.go contains an internal parseLegacyDuration that no exported API calls and a test helper for a removed format, so I want to add the new parser without preserving those.
I want to add snapshot testing to the component test suite, but jest.setup.js still includes a global mock for a deleted analytics module and a custom matcher that no test uses, so I need to wire up snapshots on the existing config.
I need to document the new /v2/orders endpoint in our API reference, but the docs source still contains a generated placeholder page for the deprecated /v1/orders endpoint and a duplicate authentication section, so I want to add the v2 page to the current navigation.
