In src/pages/Checkout.tsx, the useShippingEstimate hook calls getShippingQuote, which calls api.post('/quote'), and getShippingQuote is only used here — can you make it so I can read from the button click to the fetch result without opening three files, and drop the local quoteCache state that just mirrors the query result?
In cli/deploy.ts, the --dry-run flag is parsed in parseArgs, re-checked in runDeploy, and passed into buildManifest even though runDeploy is the only caller of buildManifest — can you handle the flag in one spot so I don't have to grep for every place it changes behavior?
Our dbt model stg_orders.sql is referenced only by int_orders_enriched.sql, which just renames three columns, and the rich model keeps a status_rank column that's also computed in the mart — can you make the path from raw orders to revenue_report readable without the intermediate model and the duplicate ranking logic?
In infra/modules/worker/main.tf there's a module that takes var.instance_type and passes it straight to a single aws_instance resource, and nothing else calls it — can you flatten it so an on-call engineer editing the scaling config sees the actual resource without an extra hop?
In ProfileViewModel.kt, the uiState LiveData is built from a _retryCount MutableLiveData that's incremented in onRetry and read in both loadProfile and renderError, and the screen also computes isRetrying from isLoading && retryCount > 0 — can you make the retry path and the spinner flag come from one source so I can tell what flips it?
The python-jsonapi client has a TransportAdapter class implemented only by RequestsTransport, and every method just forwards to session.request with the same kwargs — can you remove the adapter so users can read request construction in one file, given we're not adding a second HTTP backend?
In tests/conftest.py the db_session fixture delegates to make_engine, which delegates to load_settings, and load_settings is only called from make_engine, while test files also set os.environ['DB_URL'] directly — can you make the database the tests hit traceable from a single fixture so a CI failure doesn't require reading three setup files?
Our docs/api/errors.md says 'all timestamps are UTC' at the top, but the same sentence is repeated in auth.md, webhooks.md, and the OpenAPI descriptions, and one of them says local time — can you put that rule in a single include so the four pages stop contradicting each other?
