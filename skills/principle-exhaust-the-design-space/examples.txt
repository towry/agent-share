I need to add an inline command palette to the React dashboard in `src/components/CommandPalette.tsx`, but we've never had keyboard-driven navigation in this app and I'm unsure whether it should overlay the page, dock to the left, or float near the cursor — can you help me settle the interaction before I wire it into `AppShell`?
I'm designing the `deploy` subcommand for our Go CLI in `cmd/deploy.go`, and there's no existing pattern for whether it should stream logs by default, open a TUI progress view, or print a single summary line; I want to lock the UX before adding flag parsing and tests.
We need to choose how `etl/enrich_events.py` handles late-arriving records for the new `user_sessions` stream — rewrite the daily partition, emit a correction topic, or maintain a side table — and since there's no prior late-data path in this pipeline I don't want to lock in the schema and Airflow DAG until we know which behaves best under replay.
I'm adding multi-region failover to the Terraform stack in `infra/network/main.tf`, and because Route53 health checks, Global Accelerator, and an ALB with cross-zone routing all have different cost and latency profiles, I don't want to change production DNS until we've validated the behavior in a sandbox account.
For the new offline-first sync in `ios/Sync/SyncCoordinator.swift`, I can't tell whether conflict resolution should be last-write-wins, a merge prompt, or an operation log replay; it's the first offline feature in the app and it affects how the SwiftUI list animates, so I want to know the feel before committing to the Core Data model.
I'm writing the public API for `packages/query-builder/src/index.ts` and need to decide whether `where()` returns a new immutable builder, mutates and returns `this`, or returns a separate predicate object — the package has no existing fluent API and the choice will be hard to change once v1 ships.
I'm extending our Playwright fixture in `tests/e2e/fixtures/auth.ts` to seed tenant data, and since we've never had per-test database isolation I can't decide between transaction rollback, a template database clone, or API-based seeding; I want to understand how each handles parallel workers before rewriting 40 specs.
We're restructuring the API reference generator in `docs/generator/render.ts`, and there's no existing pattern for whether endpoints should render as one long scroll, a nested sidebar, or tabbed resource pages — the choice changes the generated HTML and anchor links, so I don't want to migrate the existing markdown until I know which layout holds up with real OpenAPI output.
